// getting user's input

// getting computer's input

function computerPlay() {
    computer_input = Math.floor(Math.random() * 3) + 1;
    return computer_input;
};
computerPlay();
console.log(computerPlay);



// compare user's and computer's input

// announce the winner

